on :
  push:
    branches:
      - main
jobs:
  deploy :
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pwd
      - run: ls -al
      - run: cat ./deploy.sh
      - run: git update-index --chmod=777 ./deploy.sh
      - run: ./deploy.sh

          
